---
export interface Props {
  variant?: 'horizontal' | 'vertical' | 'grid' | 'inline';
  showAll?: boolean;
  className?: string;
}

const { variant = 'horizontal', showAll = true, className = '' } = Astro.props;

const certifications = [
  {
    name: "HIPAA Compliant",
    description: "Healthcare data protection certified",
    icon: "🔒",
    verified: true
  },
  {
    name: "SOC 2 Type II",
    description: "Enterprise security standards",
    icon: "🛡️", 
    verified: true
  },
  {
    name: "GDPR Compliant",
    description: "EU data protection regulation",
    icon: "🌍",
    verified: true
  },
  {
    name: "ISO 27001",
    description: "Information security management",
    icon: "📋",
    verified: true
  }
];

const guarantees = [
  {
    name: "99.9% Uptime SLA",
    description: "Service level agreement guarantee",
    icon: "⚡",
    value: "99.9%"
  },
  {
    name: "24/7 Support",
    description: "Round-the-clock technical assistance",
    icon: "📞",
    value: "24/7"
  },
  {
    name: "30-Day Money Back",
    description: "Risk-free trial period",
    icon: "💰",
    value: "30 Days"
  },
  {
    name: "Setup in 24 Hours",
    description: "Quick deployment guarantee",
    icon: "🚀",
    value: "24hrs"
  }
];

const experience = [
  {
    name: "Years of Experience",
    value: "5+",
    description: "Serving businesses since 2019",
    icon: "📅"
  },
  {
    name: "Businesses Served",
    value: "10,000+",
    description: "Successful AI implementations",
    icon: "🏢"
  },
  {
    name: "Calls Handled",
    value: "2M+",
    description: "Total calls processed monthly",
    icon: "📞"
  },
  {
    name: "Customer Rating",
    value: "4.9/5",
    description: "Average customer satisfaction",
    icon: "⭐"
  }
];

const reviews = [
  {
    platform: "Google",
    rating: 4.9,
    count: 847,
    icon: "🔍"
  },
  {
    platform: "Trustpilot",
    rating: 4.8,
    count: 1203,
    icon: "⭐"
  },
  {
    platform: "Better Business Bureau",
    rating: "A+",
    count: 156,
    icon: "🏅"
  }
];
---

<div class={`trust-signals trust-signals--${variant} ${className}`}>
  {variant === 'grid' ? (
    <div class="trust-grid">
      <div class="trust-section">
        <h4 class="trust-section-title">Security & Compliance</h4>
        <div class="trust-items">
          {certifications.map((cert) => (
            <div class="trust-item">
              <span class="trust-icon">{cert.icon}</span>
              <div class="trust-content">
                <span class="trust-name">{cert.name}</span>
                <span class="trust-description">{cert.description}</span>
              </div>
              {cert.verified && <span class="verified-badge">✓</span>}
            </div>
          ))}
        </div>
      </div>

      <div class="trust-section">
        <h4 class="trust-section-title">Service Guarantees</h4>
        <div class="trust-items">
          {guarantees.map((guarantee) => (
            <div class="trust-item">
              <span class="trust-icon">{guarantee.icon}</span>
              <div class="trust-content">
                <span class="trust-name">{guarantee.name}</span>
                <span class="trust-description">{guarantee.description}</span>
              </div>
              <span class="trust-value">{guarantee.value}</span>
            </div>
          ))}
        </div>
      </div>

      <div class="trust-section">
        <h4 class="trust-section-title">Proven Experience</h4>
        <div class="trust-items">
          {experience.map((exp) => (
            <div class="trust-item">
              <span class="trust-icon">{exp.icon}</span>
              <div class="trust-content">
                <span class="trust-name">{exp.name}</span>
                <span class="trust-description">{exp.description}</span>
              </div>
              <span class="trust-value">{exp.value}</span>
            </div>
          ))}
        </div>
      </div>

      <div class="trust-section">
        <h4 class="trust-section-title">Customer Reviews</h4>
        <div class="trust-items">
          {reviews.map((review) => (
            <div class="trust-item">
              <span class="trust-icon">{review.icon}</span>
              <div class="trust-content">
                <span class="trust-name">{review.platform}</span>
                <span class="trust-description">{review.count} reviews</span>
              </div>
              <span class="trust-value">{review.rating}</span>
            </div>
          ))}
        </div>
      </div>
    </div>
  ) : variant === 'horizontal' ? (
    <div class="trust-horizontal">
      {certifications.slice(0, 4).map((cert) => (
        <div class="trust-badge">
          <span class="trust-icon">{cert.icon}</span>
          <span class="trust-name">{cert.name}</span>
          {cert.verified && <span class="verified-badge">✓</span>}
        </div>
      ))}
      {experience.slice(0, 2).map((exp) => (
        <div class="trust-badge">
          <span class="trust-icon">{exp.icon}</span>
          <div class="trust-content">
            <span class="trust-value">{exp.value}</span>
            <span class="trust-name">{exp.name}</span>
          </div>
        </div>
      ))}
    </div>
  ) : variant === 'inline' ? (
    <div class="trust-inline">
      <span class="trust-item">
        <span class="trust-icon">🔒</span>
        <span class="trust-text">HIPAA Compliant</span>
      </span>
      <span class="trust-item">
        <span class="trust-icon">⭐</span>
        <span class="trust-text">4.9/5 Rating</span>
      </span>
      <span class="trust-item">
        <span class="trust-icon">🚀</span>
        <span class="trust-text">Setup in 24hrs</span>
      </span>
      <span class="trust-item">
        <span class="trust-icon">📞</span>
        <span class="trust-text">24/7 Support</span>
      </span>
    </div>
  ) : (
    <div class="trust-vertical">
      {showAll ? (
        <>
          <div class="trust-group">
            <h5 class="trust-group-title">Security</h5>
            {certifications.slice(0, 2).map((cert) => (
              <div class="trust-item">
                <span class="trust-icon">{cert.icon}</span>
                <span class="trust-name">{cert.name}</span>
              </div>
            ))}
          </div>
          <div class="trust-group">
            <h5 class="trust-group-title">Guarantees</h5>
            {guarantees.slice(0, 2).map((guarantee) => (
              <div class="trust-item">
                <span class="trust-icon">{guarantee.icon}</span>
                <span class="trust-name">{guarantee.name}</span>
              </div>
            ))}
          </div>
        </>
      ) : (
        certifications.slice(0, 3).map((cert) => (
          <div class="trust-item">
            <span class="trust-icon">{cert.icon}</span>
            <span class="trust-name">{cert.name}</span>
            {cert.verified && <span class="verified-badge">✓</span>}
          </div>
        ))
      )}
    </div>
  )}
</div>

<style>
  .trust-signals {
    --trust-primary: var(--color-neon-blue);
    --trust-success: #00D4AA;
    --trust-bg: rgba(0, 163, 255, 0.05);
    --trust-border: rgba(0, 163, 255, 0.2);
  }

  /* Grid Layout */
  .trust-signals--grid {
    background: var(--trust-bg);
    border: 1px solid var(--trust-border);
    border-radius: 12px;
    padding: 2rem;
  }

  .trust-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
  }

  .trust-section {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .trust-section-title {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--color-text);
    margin-bottom: 0.5rem;
    border-bottom: 2px solid var(--trust-primary);
    padding-bottom: 0.5rem;
  }

  .trust-items {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  /* Horizontal Layout */
  .trust-horizontal {
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem;
    align-items: center;
    justify-content: center;
    padding: 1.5rem;
    background: var(--trust-bg);
    border: 1px solid var(--trust-border);
    border-radius: 8px;
  }

  .trust-badge {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 6px;
    white-space: nowrap;
  }

  /* Inline Layout */
  .trust-inline {
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem;
    align-items: center;
  }

  .trust-inline .trust-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--color-text-muted);
    font-size: 0.875rem;
  }

  /* Vertical Layout */
  .trust-vertical {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .trust-group {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .trust-group-title {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--trust-primary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  /* Common Elements */
  .trust-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    position: relative;
  }

  .trust-icon {
    font-size: 1.25rem;
    flex-shrink: 0;
  }

  .trust-content {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    flex: 1;
  }

  .trust-name {
    font-weight: 600;
    color: var(--color-text);
    font-size: 0.875rem;
  }

  .trust-description {
    color: var(--color-text-muted);
    font-size: 0.75rem;
    line-height: 1.4;
  }

  .trust-value {
    font-weight: 700;
    color: var(--trust-primary);
    font-size: 1rem;
  }

  .trust-text {
    color: var(--color-text);
    font-size: 0.875rem;
    font-weight: 500;
  }

  .verified-badge {
    background: var(--trust-success);
    color: white;
    font-size: 0.75rem;
    font-weight: 600;
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    margin-left: auto;
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .trust-grid {
      grid-template-columns: 1fr;
    }

    .trust-horizontal {
      flex-direction: column;
      align-items: center;
      gap: 1rem;
    }

    .trust-badge {
      justify-content: center;
      min-width: 200px;
    }

    .trust-inline {
      justify-content: center;
    }
  }

  @media (max-width: 480px) {
    .trust-horizontal {
      padding: 1rem;
    }

    .trust-badge {
      min-width: 100%;
      justify-content: center;
    }

    .trust-inline {
      flex-direction: column;
      align-items: center;
      text-align: center;
    }
  }
</style>